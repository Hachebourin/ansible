---
- name: test
  hosts: all
  become: true
  tasks:
    - name: import du role
      import_role:
        name: 'kafka'
    - name: 'Create topics'
      kafka_topics:
        state: 'present'
        name: 'test_topic_4'
        partitions: 1
        security_protocol: 'PLAINTEXT'
        bootstrap_servers: "localhost:9092"
